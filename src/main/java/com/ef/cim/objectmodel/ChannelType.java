package com.ef.cim.objectmodel;

/**
 * A {@code ChannelType} enum represents all the possible types a channel can have. A channel can
 * have one of the following types
 */
public enum ChannelType {
    FACEBOOK,
    VIBER,
    WHATSAPP,
    SMS,
    WEB,
    GENERIC
}
